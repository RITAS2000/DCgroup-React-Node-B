description: Forbidden. User is not allowed to perform this action.
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: integer
          example: 403
        message:
          type: string
          example: This recipe does not belong to you
